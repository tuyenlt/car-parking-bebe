export enum MqttTopics {
  ENTRY_GATE_CONTROL = 'entry/gate/control',
  EXIT_GATE_CONTROL = 'exit/gate/control',
  VEHICLE_ENTRY = 'vehicle/entry',
  VEHICLE_EXIT = 'vehicle/exit',
  PARKING_STATUS = 'parking/status',
}